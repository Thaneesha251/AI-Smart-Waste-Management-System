import 'package:flutter/material.dart';

class CitizenRegisterScreen extends StatelessWidget {
  const CitizenRegisterScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Citizen Register')),
      body: const Center(child: Text('Registration logic to be implemented')),
    );
  }
}
